// Elements
import { cellColor } from "./constants";
import HTMLElements from "./elements";
import { handleMenuBtn } from "./initMenuBtn";
import {
  clearCanvas,
  drawGrid,
  drawRect,
  drawLine,
  startLine
} from "./initCanvas";
import { createTimer, pushTimer, runTimerOp } from "./timers";

export default {
  cellColor,
  HTMLElements,
  handleMenuBtn,
  clearCanvas,
  drawGrid,
  drawRect,
  drawLine,
  startLine,
  createTimer,
  pushTimer,
  runTimerOp
};
